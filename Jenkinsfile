pipeline {
    agent any
    stages {
        stage('Detect language') {
            steps {
                step([$class: 'LanguageDetector', personalToken: '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', repoURL: 'https://github.com/shaN480/xtryger' ])
            }
            
        
        }
        
    }
}
